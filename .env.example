# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# For local development, use your local MongoDB or MongoDB Atlas connection string
MONGO_URI=mongodb://localhost:27017
# For production (Render.com), use MONGODB_URI instead
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/lgm_gaming?retryWrites=true&w=majority
DB_NAME=lgm_gaming

# Authentication Secrets
# Generate strong random strings for production
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
SESSION_SECRET=your-super-secret-session-key-change-this-too

# GitHub Configuration for Image Upload (Optional)
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required permissions: repo (Full control of private repositories)
GITHUB_TOKEN=your_github_personal_access_token_here
